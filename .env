# For local development (uncomment ONLY when running backend locally)
#VITE_API_URL=http://localhost:5000/api

# For production (using deployed Render backend) - ACTIVE
VITE_API_URL=https://cdps-backend.onrender.com/api

# In development the app will use the Vite proxy and a relative `/api` base.
# Leave `VITE_API_URL` commented out to ensure local dev uses the proxy.
